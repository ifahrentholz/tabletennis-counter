/**
 * Pure, UI-independent app metadata.
 *
 * This is a placeholder seam for ticket #1 (project scaffolding). Later
 * tickets introduce the real domain logic (scoring, persistence) as pure
 * modules following this same pattern.
 */
export function getWelcomeMessage(): string {
  return 'Tabletennis Counter';
}
